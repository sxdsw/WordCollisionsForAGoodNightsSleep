# Runtime deps for Word Collision for a Good Night's Sleep pipeline.
#
# The pipeline itself (corpus ingestion, KWIC, splice, traversal) uses
# only the Python standard library.
#
# nltk is needed ONLY to (re)build data/word_pool.json via
# scripts/build_word_pool.py (§5.3). After installing, download the
# WordNet corpus once:
#
#     python -c "import nltk; nltk.download('wordnet')"
#
# The word pool is a committed static file, so end users running the
# pipeline do not need nltk unless they want to regenerate it.

nltk>=3.8

# Test runner.
pytest>=7.0
